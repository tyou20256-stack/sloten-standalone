node_modules/
.wrangler/
.dev.vars
*.log
*.secret
.env
.env.local
dist/
build/
.DS_Store
Thumbs.db
# Internal-only configs (staging-bk)
wrangler.staging-bk.toml
.staging-bk-*.secret
# Golden Set per-run results — append-only artifacts, don't churn the repo
tests/golden-set/results-*.json
# Wrangler temp outdirs used by dry-run + bundle-size checks
/tmp/dryrun/
